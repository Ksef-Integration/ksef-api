required:
- type
- value
type: object
properties:
  type:
    allOf:
    - $ref: personal_permissions_authorized_identifier_type.yaml
    description: Typ identyfikatora.
  value:
    type: string
    description: Wartość identyfikatora.
additionalProperties: false
description: 'Identyfikator podmiotu uprawnionego, jeśli inny niż podmiot uwierzytelnienia.

  | Type | Value |

  | --- | --- |

  | Nip | 10 cyfrowy numer NIP |'
