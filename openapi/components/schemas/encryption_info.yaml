required:
- encryptedSymmetricKey
- initializationVector
type: object
properties:
  encryptedSymmetricKey:
    type: string
    description: 'Klucz symetryczny zaszyfrowany algorytmem RSA (Padding: OAEP z SHA-256),
      zakodowany w formacie Base64.


      [Klucz publiczny RSA](/public-keys/publicKey.pem)'
  initializationVector:
    type: string
    description: Wektor inicjalizujący (IV) używany do szyfrowania symetrycznego,
      zakodowany w formacie Base64.
additionalProperties: false
