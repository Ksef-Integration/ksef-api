required:
- type
- value
type: object
properties:
  type:
    allOf:
    - $ref: personal_permissions_context_identifier_type.yaml
    description: Typ identyfikatora.
  value:
    type: string
    description: Wartość identyfikatora.
additionalProperties: false
description: 'Identyfikator kontekstu uprawnienia (dla uprawnień nadanych podmiotom
  do obsługi faktur).

  | Type | Value |

  | --- | --- |

  | Nip | 10 cyfrowy numer NIP |'
