post:
  tags:
  - TestData
  summary: Usunięcie osoby fizycznej
  description: Usuwanie testowej osoby fizycznej. System automatycznie odbierze jej
    wszystkie uprawnienia.
  requestBody:
    content:
      application/json:
        schema:
          allOf:
          - $ref: ../../components/schemas/person_remove_request.yaml
  responses:
    '200':
      description: OK
    '400':
      description: '| ExceptionCode | ExceptionDescription | Details |

        |-|-|-|

        | 21405 | Błąd walidacji danych wejściowych. | {treść błędu z walidatora}
        |'
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/exception_response.yaml
      x-summary: Bad Request
