get:
  tags:
  - DownloadInvoices
  summary: Pobranie faktury po numerze KSeF
  description: 'Zwraca fakturę o podanym numerze KSeF.


    Wymagane uprawnienia: `InvoiceRead`.'
  parameters:
  - name: ksefNumber
    in: path
    description: Numer KSeF faktury.
    required: true
    schema:
      type: string
  responses:
    '200':
      description: OK
      content:
        application/xml:
          schema:
            type: string
    '400':
      description: "| ExceptionCode | ExceptionDescription                       \
        \               | Details                                                \
        \                                                                        |\
        \ \n|---------------|-----------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------|\n\
        | 21164         | Faktura o podanym identyfikatorze nie istnieje.        \
        \   | Faktura o numerze KSeF {ksefNumber} nie została znaleziona.        \
        \                                                            |\n| 21165  \
        \       | Faktura o podanym numerze KSeF nie jest jeszcze dostępna. | Faktura\
        \ o numerze KSeF {ksefNumber} została przetworzona, ale nie jest jeszcze dostępna\
        \ do pobrania. Spróbuj ponownie później. |\n| 21405 | Błąd walidacji danych\
        \ wejściowych. | {treść błędu z walidatora} |"
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/exception_response.yaml
      x-summary: Bad Request
    '401':
      description: Unauthorized
    '403':
      description: Forbidden
  security:
  - Bearer: []
  x-required-permissions:
  - InvoiceRead
